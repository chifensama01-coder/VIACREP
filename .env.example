# Local development uses the embedded PostgreSQL started by `npm run db`.
# Point this at Neon / Supabase / any Postgres to deploy.
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/viac"

# Signing key for the session cookie. Use a long random string in production.
AUTH_SECRET="change-me-to-a-long-random-string"
